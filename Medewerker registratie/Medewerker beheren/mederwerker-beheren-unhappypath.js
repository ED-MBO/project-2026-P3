document.addEventListener("DOMContentLoaded", () => {
  const technischeFout = true;

  if (technischeFout) {
    document.getElementById("errorMessage").style.display = "block";
    return;
  }

  // Normale laadlogica zou hier staan
});
